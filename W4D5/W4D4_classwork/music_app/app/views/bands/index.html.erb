<h1>All the bands</h1>
<ul>
  <% @bands.each do |band| %>
    <li><a href="<%= band_url(band) %>"><%= band.name %> </a></li><br>
  <% end %>
</ul>
<br>
<p><a href="<%= new_band_url %>">Add a new band</a></p>
